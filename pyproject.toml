[project]
name = "circumvent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Ronaldo Gomes", email = "rongomes15@gmail.com" }]
requires-python = ">=3.12"
dependencies = [
    "jsonschema>=4.26.0",
    "typer>=0.27.2",
]

[project.scripts]
report = "circumvent.report:app"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
